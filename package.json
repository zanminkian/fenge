{
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "install": "pnpm build",
    "prepare": "fenge install",
    "release": "pnpm -r --no-git-checks publish --tag $(test -f .changeset/pre.json && jq -r '.tag' .changeset/pre.json || echo 'latest')",
    "style": "fenge",
    "style:update": "fenge -u",
    "test": "pnpm style && pnpm -r test"
  },
  "devDependencies": {
    "@changesets/cli": "2.29.5",
    "fenge": "workspace:*",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=22.14.0"
  },
  "devEngines": {
    "runtime": {
      "name": "node",
      "version": ">=22.14.0"
    },
    "packageManager": {
      "name": "pnpm",
      "version": "10.11.0"
    }
  }
}
